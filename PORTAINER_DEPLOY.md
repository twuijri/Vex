# 🎯 خطوات النشر مع البورتات الديناميكية

## ✅ ما الجديد؟

الآن البورتات **ديناميكية**:
- إذا **8000** مشغول → Docker يستخدم بورت عشوائي
- إذا **3000** مشغول → Docker يستخدم بورت عشوائي

---

## 📋 الخطوات في Portainer:

### 1️⃣ Stacks → Add stack

### 2️⃣ اختر Repository:
```
Repository URL: https://github.com/twuijri/Vex.git
Compose path: portainer-stack.yml
```

### 3️⃣ Environment Variables (اختياري):

#### الخيار 1: استخدام البورتات الافتراضية (8000 و 3000)
**لا تضيف أي شيء** - سيستخدم 8000 و 3000 تلقائياً

#### الخيار 2: تحديد بورتات محددة
إذا تريد بورتات مختلفة، أضف:

| Name | Value |
|------|-------|
| `BACKEND_PORT` | `8080` (أو أي بورت تريده) |
| `FRONTEND_PORT` | `3001` (أو أي بورت تريده) |

#### الخيار 3: ترك Docker يختار بورتات عشوائية
أضف:

| Name | Value |
|------|-------|
| `BACKEND_PORT` | `0` |
| `FRONTEND_PORT` | `0` |

⚠️ **ملاحظة:** إذا استخدمت `0`, Docker سيختار بورت عشوائي متاح، وستحتاج تفحص أي بورت اختاره من Portainer Logs.

---

## 🚀 4️⃣ Deploy the stack

---

## 🔍 معرفة البورتات بعد النشر:

### في Portainer:
1. اذهب إلى **Containers**
2. افتح `boter_backend` → ستجد البورت المستخدم تحت **Published Ports**
3. افتح `boter_frontend` → نفس الشيء

### أو من Terminal (SSH):
```bash
docker ps | grep boter

# أو بشكل أوضح:
docker port boter_backend
docker port boter_frontend
```

---

## 🎯 الوصول للتطبيق:

بعد النشر:
- **Backend:** `http://your-server-ip:{BACKEND_PORT}`
- **Frontend:** `http://your-server-ip:{FRONTEND_PORT}`

---

## 💡 مثال:

إذا تم اختيار:
- Backend Port: `32768`
- Frontend Port: `32769`

الوصول يكون:
```
http://your-server-ip:32768  ← Backend
http://your-server-ip:32769  ← Frontend
```

---

## 🛠️ نصائح:

1. **الأفضل:** اترك Environment Variables فارغة ليستخدم 8000 و 3000 (إذا متاحين)

2. **إذا 8000 مشغول:** ضع `BACKEND_PORT=8080` مثلاً

3. **للبورتات العشوائية الكاملة:** استخدم `0` لكن ستحتاج تفحص البورت بعد النشر

---

## 📝 بعد النشر:

1. **افحص البورت المستخدم** من Portainer Containers
2. **افتح البورت في Firewall:**
   ```bash
   firewall-cmd --permanent --add-port={PORT}/tcp
   firewall-cmd --reload
   ```
3. **افتح التطبيق:** `http://your-server-ip:{PORT}`
4. **أكمل الإعداد** من لوحة التحكم (MongoDB URI, Bot Token, etc.)

---

**الآن جاهز للنشر مع البورتات المرنة! 🚀**
